name: protoc -test

on:
  push:
    branches: [ "main" ]

#设置操作系统
jobs:
  build:
      
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - name: run
        run: where python

          
